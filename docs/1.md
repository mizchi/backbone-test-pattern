# Testable Backbone pattern

This repo is build by

- mocha-phantomjs
- Browserify
- Backbone

Aimed to `Modulable` and `Testable`.

## Setup

I use Backbone

## Principles

- `chai` and `chai-jquery` for assertion
